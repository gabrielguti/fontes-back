// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       String    @id @default(uuid())
  name     String
  password String
  username String    @unique
  projects Project[] @relation(name: "ProjectToUser")
}

model Project {
  id         String   @id @default(uuid())
  title      String
  zip_code   Int
  cost       Int
  done       Boolean
  deadline   DateTime
  username   String
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  author     User     @relation(name: "ProjectToUser", fields: [username], references: [username])
}
